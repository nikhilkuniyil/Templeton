# Prompt Templates

The live prompt strings currently live in `stock_researcher/prompts.py`.

This directory is reserved for agent-specific markdown prompt files once the system moves beyond the first scaffold.
